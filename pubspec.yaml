name: firebase_auth_repository
description: A new Flutter package project.
version: 0.0.2
homepage: https://github.com/tedcrimson/firebase_auth_repository

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^1.0.2
  firebase_auth: ^1.0.1
  meta: ^1.3.0
  
  authentication_repository: ^2.0.0+1
    # path: ../authentication_repository